# AI 后端选择: zeroclaw | picoclaw
AI_BACKEND=zeroclaw

# ZeroClaw Gateway 地址
ZEROCLOW_GATEWAY_URL=http://localhost:42617
#ZEROCLOW_TOKEN=

# PicoClaw Gateway 地址
PICOCLAW_GATEWAY_URL=http://localhost:18790
# PicoClaw Token（从 ~/.picoclaw/.security.yml 的 channel_list.pico.settings.token 获取）
#PICOCLAW_TOKEN=

# 是否启用图片上传（默认 true，设为 false 则前端隐藏图片上传按钮）
#IMAGE_UPLOAD_ENABLED=true

# 是否启用记忆/知识库功能（默认 false，设为 true 则显示记忆标签页）
#MEMORY_ENABLED=false

# Web Chat 服务器端口
PORT=3332

# Gateway 配对令牌（从 config.toml 的 gateway.paired_tokens 获取）
#ZEROCLOW_TOKEN=

# 页面访问密钥（建议修改为更复杂的密钥）
ACCESS_KEY=zeroclaw2026

# 会话有效期（毫秒，默认 12 小时）
#SESSION_TTL_MS=43200000

# 允许跨域的来源列表（逗号分隔；留空表示不允许跨域）
#ALLOWED_ORIGINS=https://zero.wenyinos.com,http://localhost:3332

# 登录验证限流（默认：10次/10分钟，超限封禁15分钟）
#VERIFY_MAX_ATTEMPTS=10
#VERIFY_WINDOW_MS=600000
#VERIFY_BLOCK_MS=900000
